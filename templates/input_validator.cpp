#include <iostream>


using namespace std;

// Input Validator
// Returns 0 if input is valid, non-zero otherwise

int main() {
    // Example: Check if input contains two integers N and M where 1 <= N, M <= 100
    /*
    int n, m;
    if (!(cin >> n >> m)) return 1;
    if (n < 1 || n > 100) return 1;
    if (m < 1 || m > 100) return 1;
    return 0; // OK
    */
    
    return 0;
}
